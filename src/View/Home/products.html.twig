{% extends 'layout.html.twig' %}

{% block content %}

     {% for product in products  %}
           {{product}}

         {% if (loop.index %3) == 0 %}
                <br/>
         {% endif %}
      {% endfor %}

        {{ dump(products) }}
        
 {% endblock %}

